load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "mdtensor",
    hdrs = glob(["mdtensor/**/*.hpp"]),
    deps = [
        "@mdspan",
        # "@eigen",
    ],
    copts = [
        "-O3",
        "-fno-rtti",
        "-funroll-loops",
        "-Wall",
        "-march=native",
    ],
    defines = [
        # "MDTENSOR_USE_EIGEN",
    ],
    visibility = ["//visibility:public"],
)
