load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "mdtensor",
    hdrs = glob(["mdtensor/**/*.hpp"]),
    deps = [
        "@mdspan",
        "@eigen", # Use only when MDTENSOR_USE_EIGEN is defined
    ],
    visibility = ["//visibility:public"],
)
