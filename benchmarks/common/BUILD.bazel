load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "benchmarking",
    hdrs = ["benchmarking.hpp"],
    visibility = [
        "//visibility:public"
    ],
    deps = [
        "@google_benchmark//:benchmark",
        "//:mdtensor",
    ],
)
